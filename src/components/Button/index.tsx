import styles from "./index.module.scss";



const Button = () => {
  
  return (
    <button
      type="button"
      className={styles.button_test}
    >
      test
    </button>
  );
};

export default Button
